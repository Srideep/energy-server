# Python package
# Create and test a Python package on multiple Python versions.
# Add steps that analyze code, save the dist with the build record, publish to a PyPI-compatible index, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/python

trigger:
- main

pool:
  vmImage: ubuntu-latest
strategy:
  matrix:
    Python27:
      python.version: '2.7'
     

steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '$(python.version)'
  displayName: 'Use Python $(python.version)'
  
- task: Install Dependency
  inputs:
    script: python -m pip install --upgrade pip setuptools wheel
    displayName: 'Install dependencies'
  
- task: Install Requirments
  inputs:
    script: pip install -r requirements.txt
    displayName: 'Install requirements'
  
- task: Starting up the Server
  inputs:
    script: ./energy-server &
    displayName: 'Running the Server'
  
- task: Sending data to the listening server
  input:
    script: curl -d "starttime=1602870756&endtime=1602870872" http://localhost:8080/

    
  
 

